node_modules
dist

# Monorepo uses pnpm only — npm lockfile in site/ makes Vercel run npm install and breaks workspace:*
site/package-lock.json
.DS_Store
.env
.env.local
.env*.local
*.log
.vercel
.next

# UI template typecheck cache (regenerated locally)
ui/.template-tsc-cache/

# Cursor internal working files
**/.cursor/plans/
**/.cursor/projects/

