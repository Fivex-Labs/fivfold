import { IsString, IsNotEmpty, IsNumber, IsArray, IsOptional } from 'class-validator';

export class VotePollDto {
  @IsString()
  @IsNotEmpty()
  pollId!: string;

  @IsNumber()
  optionIndex!: number;
}
